{
  "name": "dsh-visual-workflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DSH host + client 双面可视化多 Agent 工作流设计器插件（模式一编排执行 / 模式二后台服务）",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "lib/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/types/index.d.ts",
      "default": "./lib/index.js"
    },
    "./client": {
      "types": "./lib/types/client/index.d.ts",
      "default": "./lib/client.js"
    },
    "./service-runner": {
      "default": "./lib/service-runner.js"
    },
    "./serve.patch.yml": "./serve.patch.yml",
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "serve.patch.yml",
    "assets/models/bge-small-zh-v1.5",
    "README.md"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web",
      "inject": [
        "@deepseek-ai/dsh-client-runtime"
      ]
    }
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "*",
    "@deepseek-ai/schemastery": "*"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0"
  },
  "optionalDependencies": {
    "mysql2": "^3.15.0",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@deepseek-ai/schemastery": "^3.18.1",
    "@types/jsdom": "^28.0.3",
    "@types/node": "26.2.0",
    "@types/pg": "^8.23.1",
    "@types/react": "^19.2.8",
    "@types/react-dom": "^19.2.3",
    "jsdom": "29.1.1",
    "lightningcss": "1.33.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.host.json --noEmit && tsc -p tsconfig.client.json --noEmit && tsc -p tsconfig.test.json && tsc -p tsconfig.test.client.json",
    "build": "node scripts/build.mjs",
    "test": "vitest run --pool=threads",
    "client-smoke": "node scripts/client-smoke.mjs",
    "check": "pnpm typecheck && pnpm test && pnpm build && pnpm client-smoke",
    "verify": "pnpm typecheck && pnpm build && pnpm test && pnpm client-smoke"
  }
}
