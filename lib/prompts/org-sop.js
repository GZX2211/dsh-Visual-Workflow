// src/host/prompts/org-sop.ts
//
// 自主编排「规划 SOP」三层文本（自主编排实施方案 §10 P2；决策台账 D-19）：
//   - L1 图语义（ORG_SOP_L1_GRAPH_SEMANTICS）：图模型（节点种类 / 连线语义 / 检查器硬规则）的稳定说明；
//   - L2 模式库（ORG_SOP_L2_PATTERN_LIBRARY）：常见编排模式的稳定说明（适用场景 + 图形态 + 要点）；
//   - L3 用户 SOP 注入点：本轮**只留位**（规划变体入参 dynamic.userSop 默认空字符串 = 不组装该段）；
//     SOP 的用户可编辑化按 D-19 后期单独实施，本轮只保证注入位置稳定存在。
//
// 为什么单独成文件：L1/L2 是**长稳定文本**（字节稳定 → 前缀 KV 缓存友好），与规划变体的
// 组装逻辑（org-plan.ts）职责分离；两段文本也可被其他变体复用（如运行期变更说明）。
//
// 纯度：本文件只有常量字符串，无 IO、无时钟、无随机源（架构文档 §13 提示词工程）。
/** L1 图语义段（稳定前缀；节点种类 / 连线语义 / 检查器硬规则）。 */
export const ORG_SOP_L1_GRAPH_SEMANTICS = `组织规划图语义（L1：图模型与硬规则，稳定说明，规划前必读）：

一、节点种类（9 种，kind 字段为判据）
1. start：启动阶段节点，模式一入口；全图恰好 1 个，只允许流程出线；
2. end：结束阶段节点，模式一出口；全图恰好 1 个，只允许流程入线；
3. pause：暂停阶段节点，运行到此落检查点并等待恢复；必须同时有流程入线与流程出线；
4. agent：子代理执行节点（普通执行单元），承担一件可独立交付的任务；
5. parent：父代理节点，既是调度中枢也是执行单元；被流程线连接时父代理亲自执行该节点任务；
6. group：协作组节点（组卡片），组内成员并行开工，成员 id 必须都存在于画布节点集合；组卡片自身必须至少一条流程线；
7. proxy：虚拟节点（主节点别名引用），无独立配置，只能引用角色节点（agent/parent）；用于阶段入口与里程碑闸门，指向父代理的虚拟节点需要流程入口；
8. file：受管文件数据源节点，必须配置实际文件；
9. database：数据库数据源节点，必须配置路径或连接。

二、连线语义
1. flow（流程线）：执行顺序与父子调度关系；可挂 condition 条件分支，同一源节点同一流程出上的条件线必须成对（有「通过」就必须有「不通过」）；
2. ctx（上下文线）：把上游产出注入下游节点；来源只能是角色节点、受管文件节点或启动节点；
3. db（数据库线）：数据库节点之间连线；目标必须是数据库节点。

三、图检查器硬规则（error 级会阻断落盘，必须先修好再重新提交）
- 恰好 1 个 start 与 1 个 end，且 start 无流程入线、end 无流程出线；
- start 的流程出线至少 1 条，且目标必须是可执行单元（agent / parent / group）；
- end 的流程入线至少 1 条，且来源必须是可执行单元（agent / parent / group）；
- 不允许悬空节点：每个节点至少要有一条流程线或上下文/数据库线；
- 不允许流程环；不允许「有流程入线但从 start 沿流程不可达」的孤岛段；
- 不允许协作组无成员、成员 id 不存在、或组卡片没有任何流程线；
- 不允许虚拟节点引用不存在或非角色节点的主节点；
- 数据节点必须配置完整（数据库路径/连接、受管文件实际文件）。
`;
/** L2 模式库段（稳定前缀；六种常见编排模式）。 */
export const ORG_SOP_L2_PATTERN_LIBRARY = `组织规划模式库（L2：常见编排模式，稳定说明）：

模式 1 顺序流水线（pipeline）
- 适用：步骤之间强依赖，后一步需要前一步的产出。
- 图形态：start 到 agent(A) 到 agent(B) 依次相连，最后到 end；ctx 线按需从上游指向下游。
- 要点：每一步只承担一件可验收的交付；不要为了并行而拆分没有独立价值的步骤。

模式 2 并行扇出与汇总（fan-out / join）
- 适用：多个互不依赖的子任务，最后需要统一收敛。
- 图形态：start 之后由同一节点分别连出多条流程线到并行 agent，再由并行 agent 汇聚到同一个汇总节点（parent，或 end 之前的汇总 agent）。
- 要点：只有真正无依赖的分支才并行；并行宽度受元参数「单列并行分支上限」约束。

模式 3 协作组并行（collab group）
- 适用：需要多名角色在同一议题上互相通信、交叉验证（如多视角评审）。
- 图形态：group 卡片包含若干成员 id，组卡片串在流程上；组内成员并行启动，组内通过 wf_ask_agent 互相提问。
- 要点：组内人数受元参数「组内人数上限」约束；组卡片必须有流程线，成员必须真实存在。

模式 4 评审与返工分支（producer / reviewer）
- 适用：产出质量需要独立校验，且校验不通过时要返工。
- 图形态：执行节点到评审节点；评审节点用**成对条件分支**分别指向下游（通过 pass）与**返工修复节点**（不通过 fail），返工修复节点再汇入下游（与通过分支在质量门控或汇总节点重新汇合）。
- 要点：条件线必须成对出现（有 pass 必有 fail）；**流程必须保持无环 DAG**——禁止把「不通过」分支回指上游节点，回指会被检查器判为 flowCycle 并阻断落盘。返工后需要重新验证时，新增一个「复测/门控」节点收敛两条分支，不要建回头线。

模式 5 里程碑闸门（milestone gate）
- 适用：长流程需要在关键节点由父代理亲自复核进展，再决定是否继续扩张。
- 图形态：在关键位置插入指向父代理的 proxy 虚拟节点（父代理亲自跑一轮复核），复核通过后由父代理显式标记该闸门完成。
- 要点：闸门次数受元参数「父代理闸门上限」约束，且不含首次编排；闸门不自动通过。

模式 6 数据注入（data injection）
- 适用：节点需要读取受管文件或数据库。
- 图形态：file / database 数据节点用 ctx 线或 db 线接入执行节点。
- 要点：数据节点必须配置完整，否则检查器直接报错。
`;
//# sourceMappingURL=org-sop.js.map