# 开发环境说明（内部协作文档）

> 本文件记录本机开发环境事实，供各阶段开发子代理引用；不随包分发（不在 package.json `files` 中）。

## 1. 工具链

| 工具 | 版本 | 备注 |
|------|------|------|
| node | v24.17.0 | |
| pnpm | 11.22.0 | 包管理器 |
| git | 2.53.0.windows.2 | 仓库主分支 main |
| dsh CLI | 0.1.1-rc.2（全局 `C:\Users\GZX\AppData\Roaming\npm\dsh.ps1`） | 全局安装 `@deepseek-ai/dsh@0.1.1-rc.2` |
| python | **不可用** | R-03 的 ONNX 转换无法走本机 optimum 导出，需走官方 ONNX 产物获取或记录降级说明 |

## 2. 关键路径

| 用途 | 路径 | 权限 |
|------|------|------|
| 本项目工作目录 | `D:\AiCoding-Gzx\HarnessPlugin\dsh-visual-workflow` | 读写 |
| 官方源码（只读取证，§8 索引） | `D:\AiCoding-Gzx\HarnessPlugin\deepseek-harness-dsh-v0.1.1-rc.2`（无 .git 的拷贝） | **只读** |
| 旧项目（只读复用） | `D:\AiCoding-Gzx\HarnessPlugin\VisualWorkflow` | **只读** |
| 嵌入模型资产源（PyTorch） | `D:\AiCoding-Gzx\models\backend\models\bge-small-zh-v1.5`（safetensors 91MB、tokenizer.json 等；**无 ONNX 产物**） | 只读 |

## 3. 版本镜像约定（与官方仓库 devDependencies 对齐）

| 包 | 版本 | 用途 |
|----|------|------|
| typescript | ^6.0.3 | 双 program 编译（官方同款） |
| tsdown | ^0.22.2 | client bundle 构建（复用官方 tsdown.client.ts 模式） |
| vitest | ^4.1.8 | host/client 单测 |
| jsdom | 29.1.1 | client 单测环境 |
| @types/jsdom | ^28.0.3 | jsdom 类型 |
| @huggingface/transformers | 官方架构文档指定（唯一第三方运行时依赖） | 本地嵌入推理 |

- react / react-dom 仅为 devDependencies（client bundle 由官方平台模块表提供，构建期从 `lib/` 找回资源）。
- 不得声明任何 `@deepseek-ai/*` 运行时依赖（W-05）；peerDependencies 仅 `@deepseek-ai/cordis: *`。

## 4. 旧项目结构速览（只读复用，按清单 §9）

```
D:\AiCoding-Gzx\HarnessPlugin\VisualWorkflow\
├── lib/  agent-runner.js / documents.js / flow-model.js / flow-store.js / index.js
│         mcp-registry.js / memory.js / orchestrator.js / plugin-catalog.js / remote.js
│         templates.js / tools-registry.js / transfer.js / wf-tools.js / client.rev
├── src/client/  entry.js / studio.js / graph-canvas.js / graph-model.js / node-panels.js
│                run-view.js / combo-manager.js / i18n.js / styles.js
│                components/{confirm-dialog,inspector,left-panel,toolbar}.js
│                lib/{files,node-utils,remote}.js
└── scripts/  build.mjs 等
```

旧项目脚本（参考，非照搬）：`build`（esbuild）、`test`（node --test）、`smoke`（client 冒烟）、`check`。

## 5. 官方源码 §8 索引抽样核验（已确认存在）

`packages/client/tsdown.client.ts`、`packages/subagent/subagent/README.md`、`packages/core/tools/README.md`、`packages/host/webserver/README.md`、`packages/core/agent/src/runtime-types.ts`、`packages/preset/agent-presets/README.md`、`packages/mcp/mcp-client/README.md`、`packages/session/session-persistence-jsonl/README.md`、`packages/bundle/headless/README.md`、`packages/boot/cmdline/README.md`、`packages/client/ui-conversation/src/client/contract/slots.ts`、`packages/core/agent/README.md`、`packages/fs/tool-fs`、`packages/test-support/client-runtime`、`packages/storage/storage-json` —— 全部存在。

## 6. 网络

- npm registry 可用（`pnpm view` 验证通过）。
- 官方 ONNX 模型产物（BAAI/bge-small-zh-v1.5 onnx/ 目录）可经网络获取（T-004 需验证可用性；失败则记录明确降级说明）。

## 7. 提交与日志约定

- 每个开发阶段结束：主代理复跑全量测试 → `git commit`（功能提交，消息形如 `P0X: <阶段名>（T-xxx）`）→ logs.md 补日志（引用该提交前 7 位哈希）→ `git commit`（`docs: 更新 logs.md（P0X）`）。
