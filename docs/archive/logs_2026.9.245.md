## 2026.09.05 - AI 开发日志

- git版本：[f03ef02] [v0.9.4] [22:08]（feat：底栏 Tag 移到左侧（竖排/文字横向）；折叠切换改 3 态；底栏边界线真正横向可拖；修复顶栏溢出）
  - 【折叠/切换改用 3 态循环】按用户裁决：左展开→切换底栏→收起底栏→左展开（`PANEL_CYCLE_LEN=3`）；`PANEL_MODE_LEFT/BOTTOM/NONE`，`nextPanelMode=(mode+1)%3`；`leftPanelOpenOf=0`、`bottomPanelOpenOf=1`、`panelsFullyCollapsedOf=2`；run/分栏落入 `PANEL_MODE_NONE`。
  - 【底栏 Tag 移到左侧】Tag 区从顶部改为底栏【左侧】竖排栏（`.wf-bottombar__tags`：`flex-direction:column`，宽 88px，右分隔线），但每个 Tag 文字是【横向】的（工作流/角色/数据/其他，不显示图标）；右侧为内容滚动区（分区标题 + 横向换行卡片）。
  - 【底栏边界线真正横向】此前 `.wf-splitter--horizontal` 因与基础 `.wf-splitter`（`width:9px;col-resize`）同优先级且基础规则在后而失效，露出竖向拖动条；改为双类选择器 `.wf-splitter.wf-splitter--horizontal`（`min-width:0;width:auto;min-height:8px;height:8px;cursor:row-resize`）覆盖，位于底栏顶部、上下拖调整高度。
  - 【顶栏/工具栏溢出修复】此前 `.wf-root` 由 grid 误改为 flex 列，导致标题栏/画布控制栏按钮超出容器；恢复为 grid（48px / 主区 / 底栏区 三行），标题栏与工具栏布局回到原状，按钮不再突出。
  - 【测试】更新 bottom-panel.test.tsx：3 态循环、Tag 区位于底栏左侧、水平边界线拖拽；typecheck + 814 测试（注：host atomic/orchestrator 在 Windows 并发下偶发超时，属已知环境抖动，单独重跑通过）+ client-smoke。

- git版本：[372d73a] [v0.9.3] [21:23]（feat：工作台 UI/交互优化——新增底栏与左栏切换；右侧属性栏改为选中驱动显隐）
  - 【底栏（新增组件 BottomPanel，独立文件不复用左栏）】与左栏相互切换以优化界面空间；Tag 区以图标显示工作流/角色/数据/其他 4 个 tag（不显示文字），切换 Tag 时下方内容随动（实例/模板、角色模板、文件/数据库、阶段/协作组）；卡片仅显示名称（不显示描述与图标），横向 flex-wrap 动态追加排（底栏够宽可无限追加排）；支持拖拽入画布；上边界线可上下拖动调整高度。内容/选中/拖拽逻辑抽取到 library-model.ts（纯函数）与左栏共用，保证两份显示完全一致——本次纯前端显示/交互改动，无后端与数据改动。
  - 【折叠/切换按钮 6 态循环】不再控制右侧栏，只控制左栏/底栏展开、折叠、切换：左展→切底→底收→底展→切左→左收→左展（默认左栏展开）。面板几何改为 `mode`（循环位置 0..5）+ 左右宽度 + 底栏高度；左/右/底三向拖动调整。
  - 【右侧属性栏显隐】默认隐藏（折叠），仅当选中「具备属性」的对象时自动展开，选中消失（点画布空白）即收起；判定 = 编辑对象存在且其属性栏类型非「阶段」——阶段节点/侧栏阶段卡片不弹；父代理模板也具备属性（修正原 `selectEditor(null)` 不弹逻辑）；其余（实例/工作流/服务/模板/角色/文件/数据库/协作组/连线/画布角色节点含父代理节点/虚拟节点）都弹。
  - 【左栏 Tag 图标化】工作流/角色/数据/其他 4 个 Tab 由文字改为图标展示。
  - 【测试】新增 tests/client/bottom-panel.test.tsx（6 态循环/底栏 Tag 与卡片/属性栏显隐判定/父代理模板/底栏拖拽）；Studio.test.tsx 适配图标 Tag；`pnpm verify` 全绿（typecheck + build + 814 测试 + client-smoke）。

- git版本：[c3b25d6] [v0.9.2] [16:29]
  - 【工作台全局化】工作台不再绑定单一 DSH 会话：实例/服务列表 = 全部会话实例集合（`listWorkflows/listServices/activeRuns` 的 sessionId 改为可选，缺省返回全量）；当前主会话（会话树根）对应实例打「当前」标签（LeftPanel）；进入时按当前会话自动选中（运行中>暂停>最新，无则空白画布）；打开期间会话切换仅更新标签不跳转画布。所有实例级操作（运行/停止/历史/恢复/删除/导出/外部同步）改用实例自身 sessionId 归属。
  - 【每会话单实例（按模式各一）】目标会话已有实例且未勾选「开启新会话」→ 弹「新运行的工作流将会覆盖旧的工作流」二次确认；确认后复用旧实例 id 更新内容（revision+1，运行历史连续）；定时任务 current-session 触发时自动覆盖再运行（engine.fire + instantiate.ts 覆盖纯函数）。
  - 【「开启新会话」语义重构】**一次性创建选项**：仅模板态显示；勾选后先经新 `EP_CREATE_SESSION` 端点（官方 agents.create 不传 parentSession 即无父根会话=主会话；workspacePath 校验即新会话 cwd，缺省继承创建者会话 cwd）新建主会话并绑定实例；`startNewSession`/`workspacePath` 字段退役（保存端点剥除、实例化不继承、运行期新会话分支与 StartRunOptions 移除）；模板/实例文档不再存储。
  - 【运行唯一逻辑】run.sessionId === instance.sessionId 恒成立（`run` 端点不再接受 startNewSession；resume/stop/history 均按实例会话归属）。
  - 【模式二服务】服务实例同样每会话单实例 + 模板态新会话绑定；服务级新会话（openai-api 每请求新建）字段旧数据兼容读取，新实例保存即剥除（回退 userId 固定会话+断点续跑）。
  - 【测试】新增 tests/client/workbench-global.test.tsx（仅模板态开关/覆盖确认复用 id/新会话绑定/当前标签）；host 侧更新 api/flow-store/scheduler-engine/instantiate/orchestrator/shared-contract 断言（52 端点、跨会话列表、退役字段剥除、覆盖语义）。
  - 注：Windows 全量并发跑时 atomic/orchestrator-watchdog/service-manager 偶发超时（真实锁/定时器/fork 环境抖动），单独重跑均通过。

## 2026.09.04

- git版本：[1d64f57] [v0.9.1] [17:14]（fix：新会话预设挂载缺失 / feat：组合管理标签一键开关）
  - 【根因修复：开启新会话后工具面板只剩注册工具/MCP 工具】`CordisSessionProvider.createSession`（scheduler/session-provider.ts）只把 `agentPreset: 'standard'` 写入会话 header，但未在 `agents.create` 的 `setup` 钩子里执行 `agentPresets.mount`——新会话根 Agent 只继承全局层（宿主+插件 wf_*+MCP）工具，官方 `standard` 预设工具（bash/pwsh/fs/jobs/skill/goal/subagent/workflow/web…）全缺失。按官方 api-proxy `composeAgent` 同源修复：先 `resolve` 得 id 供 header 记录，再在 `setup` 内 `mount`；`agentPresets` 服务缺失时降级为不挂载（无预设 roster 部署容错，模式二 headless 进程无 agentPresets 时保持原行为）。
  - 【功能：组合管理标签一键开关】工具 Tab 标签栏（[全部]/[官方工具]/[MCP 服务器]）右侧新增「一键开启/一键关闭」按钮：仅作用于当前激活标签命中的工具集合（`filterToolNamesByTag` 收窄），不影响其他标签或官方工具；走新增 `toolSwitchPutMany` 批量端点（`ToolSwitchStore.setDisabledMany` 单次原子落盘 + 内存快照刷新）；官方保留传输名 run_code 静默跳过。UI 新增 i18n 键（一键开启/关闭、批量成功提示、tooltip）与 `wf-combo-tag__bulk` 样式。
  - 【新增测试】session-provider 新会话预设挂载（4）、tool-switches setDisabledMany（2）、api 批量端点（3）、ComboManager 标签一键开关 jsdom（2）；协议契约 50→51。
  - 【回归验证】pnpm verify 全绿：typecheck 4 program、全量 59 文件 796 用例、build、client-smoke 全部通过。
  - 【待跟进】组合管理「全部」标签不显示一键开关按钮（无明确批量语义）；模式二服务进程 headless 组合无 agentPresets，服务级新会话仍按全局层工具运行（如需官方工具应在 serve patch 层装配 agent-presets）。

## 2026.09.02

- git版本：[60f879b] [v0.9.0] [23:00]（feat：父代理工具白名单全局开关 / 动态MCP Tag筛选 / 启动时开启新会话+工作区 / 父代理执行者模式）
  - 【需求背景】父代理此前无工具过滤（toolFilter 是 startContinuable 子代理专用）；组合管理需为工具卡片加开/关按钮；工具列表上方需动态 Tag（官方工具 + MCP server）筛选；画布/定时任务/API服务需支持「启动时开启新会话 + 选择工作区」；父代理被流程线连接后应作为执行单元先执行自身任务再调度。
  - 【官方源码取证】① system-prompt/assemble 瀑布的 assembly.tools 是模型可见工具 Schema 权威来源（dsh-tools wireSchemas → view(scope).visible），dsh-scope scopeTarget 过滤语义：unscoped ctx 监听对所有 agent 组装生效 → 全局瀑布改写可行；② tools.restrict 需 agent-scoped ctx、只过滤继承面、只对已存在 agent 生效，不满足「不点运行也全局生效」；③ 官方 mcp-client publicToolName = mcp__<server>__<tool>（非法字符替换+哈希）；④ dsh-sandbox-policy workspaceRoot = session.header.cwd（工作区路径输入即沙箱联动闭环）；⑤ session.events 的 assistant/message 事件可回写父代理节点产出。
  - 【实现】① tool-switches.ts（tool-switches.json 原子持久化 + 全局 assemble 瀑布过滤 + 纯函数 filterToolsInAssembly）+ toolSwitches/toolSwitchPut 端点 + pluginCatalog 附带 disabledTools + resolveAgentTools 二次剔除（含 db-in 注入的 wf_db_query）；② tool-tags.ts 纯函数（全部/官方/动态 MCP，规范化反查 serverName）+ ComboManager Tag 胶囊栏与开/关按钮（置灰禁勾选、关闭自动移出组合草稿）；③ startNewSession/workspacePath 落 WorkflowDocument/WorkflowTemplate/ServiceState/ScheduledTask，mode1 startRun 经 sessionProvider 新建会话（EP_RUN 返回实际 sessionId，前端 run.sessionId 跟随轮询/停止/恢复），定时任务直传 cwd，模式二每请求新建会话（createSession 缺失回退映射）；保存端点 stat 校验目录存在；④ 父代理执行者模式：parentExecutorOf（flow-in 命中）→ prepareParentExecutor（快照 running + buildNodeBlocks 任务块注入指令尾段「你的节点任务」）→ 首次 wf_run_node/wf_finish 标记 ok（输出取 latestRootAssistantText，新增 AgentHost 缝）→ terminalize 失败语义；编排指令 parentAsNode 双层措辞。
  - 【新增测试】tool-switches（9）、tool-tags（11）、instantiate-new-session（5）、执行者模式（5）、新会话 startRun（3）、scheduler workspacePath（2）、openai-api 服务级新会话（2）、ComboManager Tag/开关 jsdom（4）；端点契约 48→50。
  - 【回归验证】pnpm verify 全绿：typecheck 4 program、全量 58 文件 786 用例、build、client-smoke 全部通过。
  - 【待跟进】新会话运行的工作流在重启工作台后不属于当前会话活跃 run（activeRuns 按会话过滤），不会自动选中实例；运行历史面板展示的是实际执行会话（run.sessionId）的历史。