# 依赖与构建产物

# 依赖
node_modules/
.pnpm-store/

# 构建产物（当前阶段 lib 为构建产物，忽略；SKILL.md §7.1 备选分发策略若需
# 「可提交 lib」时再单独调整）。根级锚定：src/client/lib/ 是源码目录，不得忽略。
/lib/

# TypeScript 增量产物
*.tsbuildinfo

# 测试产物
coverage/
imgs/

# 临时验证目录（T-002 Loader 验证：临时 DSH_HOME / scratch profile，避免污染用户全局环境）
.tmp/

# 编辑器 / 系统临时
.DS_Store
Thumbs.db
*.log

# 本地浏览器 E2E 临时目录
.edge-tmp/

# AI 提示词工程文件
prompt/

# CodeGraph 本地索引（codegraph init 生成，不提交）
.codegraph/

# docs
archive/
bug/