# 模式二服务进程 serve 层模板（顶层数组）。
# 渲染实现见 src/host/service/serve-patch.ts（renderServePatch）：
# 服务管理器启动时动态渲染到 <dataDir>/services/<serviceId>.serve.patch.yml
# （含 headless-runner disabled + webserver 行 + 插件行，插件 name 为绝对 file URL）。
# 本文件为空数组：仅保证 exports["./serve.patch.yml"] 指向的文件真实存在。
[]
